# Workspace audit — projects/top-coder-challenge

Date: 2026-09-01. Part of a read-mostly sweep across `~/dev` (full write-up: `~/dev/WORKSPACE_AUDIT_2026-09-01.md`).
Nothing was committed or pushed; every change below is sitting in the working tree for you to review with `git diff` / `git status`.

## What changed here

- **Added `.gitignore`** (this repo had none). Baseline: `.env*` (with `.env.example`/`.env.template` allowed), OS/editor cruft (`.DS_Store`, `.vscode/`, `.idea/`), logs/tmp, plus the language-specific cache/build/dependency dirs for what is in the tree.
- Added `*.pkl` / `*.joblib` to the ignore list: eight trained-model pickles (~700 MB on disk) are tracked in this repo and the branch is 8 commits ahead of `origin` — pushing as-is would upload all of them.

## Files that are tracked by git but now match an ignore rule

Adding a pattern to `.gitignore` never untracks a file that is already committed. These are still tracked; if they should not be, run `git rm --cached <path>` (removes from the index only — the file stays on disk) and commit:

- `__pycache__/advanced_ml_algorithms.cpython-313.pyc`
- `__pycache__/calculate_reimbursement.cpython-313.pyc`
- `__pycache__/ensemble_algorithm.cpython-313.pyc`
- `__pycache__/exact_match_gb.cpython-313.pyc`
- `__pycache__/experiment_tracker.cpython-313.pyc`
- `__pycache__/test_gradient_boosting.cpython-313.pyc`
- `balanced_model.pkl`
- `enhanced_private_model.pkl`
- `fast_optimized_model.pkl`
- `final_private_model.pkl`
- `generalizable_model.pkl`
- `optimal_simple_model.pkl`
- `ultra_precise_gb_model.pkl`
- `ultra_private_model.pkl`

## Pre-existing uncommitted work (not touched)

`git status` showed 15 modified/untracked path(s) here before this audit started. They were left exactly as found.

